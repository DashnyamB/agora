import React, { useEffect } from "react";
import "./style.scss";

function Header(props) {
    useEffect(() => {
    }, []);
    return (
        <nav>
            <div>
                hahaha
            </div>
        </nav>
    );
}

export default Header;
